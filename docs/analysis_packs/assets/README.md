# Analysis Pack Assets

This directory contains small, paper-facing assets needed to compile the
standalone IEEE experiment section. Raw solver logs, run directories, cut
pools, diagnostics, and generated experiment outputs belong under ignored
`results/` directories instead of this tracked asset folder.

